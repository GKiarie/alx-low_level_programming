#include "main.h"

/**
  * _abs - comput the absolute value of an int
  * @n: input
  * return: Absolute value of integer
  */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
