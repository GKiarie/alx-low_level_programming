#include "calc.h"

/**
 * sub - function that subtracts two ints
 * @a: first input
 * @b: second input
 * Return: diff btn two integers
 */
int sub(int a, int b)
{
  int diff;
  
  diff = a - b;
  return (diff);
}
