#include "calc.h"

/**
 * div - function that divides two ints
 * @a: first input
 * @b: second input
 * Return: quotient of two integers
 */
int div(int a, int b)
{
  int quot;
  
  quot = a / b;
  return (quot);
}
