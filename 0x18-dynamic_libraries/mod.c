#include "calc.h"

/**
 * mod - function that gives rem btn two ints
 * @a: first input
 * @b: second input
 * Return: remainder on dividing two integers
 */
int mod(int a, int b)
{
  int rem;
  
  rem = a % b;
  return (rem);
}
