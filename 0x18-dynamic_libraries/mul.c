#include "calc.h"

/**
 * mul - function that multiplies two ints
 * @a: first input
 * @b: second input
 * Return: product of two integers
 */
int mul(int a, int b)
{
  int prod;
  
  prod = a * b;
  return (prod);
}
