#include "calc.h"

/**
 * add - function that adds two ints
 * @a: first input
 * @b: second input
 * Return: sum of two integers
 */
int add(int a, int b)
{
  int sum;
  
  sum = a + b;
  return (sum);
}
